/**
 * Franja superior del shell admin: marca del sidebar + `<header>` principal.
 * También documenta el 44px usado en `h-[calc(100vh-44px)]` (workspace editor).
 */
export const adminHeaderRowClassName = "h-11 min-h-[44px]";
